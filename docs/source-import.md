# MyCards package source import

The first import of `owl_haptics`, `owl_marquee`, and `spatial_confetti` came from the tracked source at MyCards commit `0c2f2e0e9d01ceccc4e2bb5ca0f8f07d22645ba2` (`https://cnb.cool/owlllwo/mycards`).

The import used Git's committed tree, excluding ignored build output and machine-local files. A byte comparison of all 264 tracked files at those three package paths against the imported copies found 0 missing and 0 changed files before subsequent packaging/documentation edits. This record establishes source provenance, not redistribution clearance for every asset.
