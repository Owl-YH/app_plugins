name: owl_marquee_example
publish_to: none

environment:
  sdk: '>=3.12.2 <4.0.0'
  flutter: '>=3.44.6'

dependencies:
  flutter:
    sdk: flutter
  owl_marquee:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  config:
    enable-web: true
  uses-material-design: true
